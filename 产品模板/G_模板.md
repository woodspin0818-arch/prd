---
模板编号：G（功能模块规格）
模板名称：功能模块规格（按功能维度组织）
适用场景：产品规划 → UI设计 → 前端开发 → 后端开发 → 测试验收，全链路使用
读者：产品经理、UI设计师、前端开发、后端开发、测试
版本：V2.2
日期：2026-05-17
作者：产品架构师 Agent
状态：模板
---

# [模块名称]：功能模块规格

> 📄 **文件命名规则**：创建功能文档时，文件名格式为 `T{X}-{Y}_{功能名称}（MMDD）.md`，其中 `MMDD` 为创建日期的月日（如 `0529` 表示5月29日）。示例：`T0-3_商品功能（0529）.md`

---

> ⚠️ **填写原则**：本文档中所有模块/章节均为选填项，按实际情况保留或删除。但保留的每个模块必须填写到**可以交付开发**的程度。遇到需求不清晰的点，务必向用户追问确认，直到功能明确为止，不要自行猜测。

---

## 模块概览

> 填写指引：明确本模块覆盖哪些功能。功能ID 和 页面ID 引用自《A01_用户侧全局说明》（功能清单与页面框架）。

| 项目 | 内容 |
|------|------|
| 模块名称 | [如：首页模块 / 签到模块 / 用户模块] |
| 功能入口 | [如：底部导航栏 → 首页 Tab / 个人中心 → 我的订单] |
| 覆盖功能 | TX-X001, TX-X002（引用 A01 功能清单） |
| 覆盖页面 | 01, 01-A（引用 A01 页面拓扑图） |
| 时间 | YYYY-MM-DD（本模块首次创建/最近一次迭代的时间） |
| 依赖模块 | [列出本模块依赖的其他模块，如"依赖用户模块的登录态"] |

> ⚠️ 页面ID 和 功能ID 必须在 A01 中存在，否则本模块规格无效。

> ⚠️ **系统预置规则（选填）**：如有需要系统上线时自动生成的默认数据（如默认 Banner、默认广告位），在此说明预置规则和兜底策略。

---

## 功能 TX-X001：[功能名称]

---

### 1. 需求背景与目标 (Why)

> 填写指引：一句话交代"为什么要做"，让团队理解业务价值，在遇到细节分歧时做出正确的判断。

[如："目前用户在结账页面流失率高达30%，本功能通过新增快捷支付方式降低流失率"]

---

### 2. 业务流程与逻辑 (How - 宏观)

> 填写指引：用图表的形式直观展示功能的运转逻辑，这是研发和测试最看重的部分。

#### 2.1 用户路径图 (User Flow)

> 填写指引：用户从进入页面到完成目标，需要经过哪些页面和点击哪些按钮？用 ASCII 流程图或编号步骤描述。

```
[用户路径示例]

  进入页面01         点击按钮A          填写表单           提交成功
  (首页)      ───►   (发起操作)   ───►   (信息录入)  ───►  (结果页)
     │                   │                  │                 │
     │ 条件不满足        │ 校验失败         │ 信息不完整      │ 
     ▼                   ▼                  ▼                 ▼
  [引导页/提示]      [错误提示]         [标红提示]       [再次操作]
```

> 也可用步骤列表描述：

| 步骤 | 触发动作 | 所在页面 | 前置条件 | 后续页面 |
|------|----------|----------|----------|----------|
| 1 | [进入页面] | 页面XX | [条件] | - |
| 2 | [点击某按钮] | 页面XX | [条件] | 页面YY |
| 3 | ... | ... | ... | ... |

#### 2.2 状态流转图

> 填写指引：如果功能涉及状态变化（如订单、审批、任务），需要列出所有状态及其触发条件。

```
[功能名称] 状态流转图：

  ┌──────────┐     触发条件A     ┌──────────┐
  │  状态1    │─────────────────►│   状态2   │
  │ (初始态)  │                  │ (中间态)  │
  └──────────┘                  └──────────┘
       │                             │
       │ 触发条件C                   │ 触发条件B
       ▼                             ▼
  ┌──────────┐                  ┌──────────┐
  │  状态4    │◄─────────────────│   状态3   │
  │ (终态)   │    触发条件D      │ (终态)    │
  └──────────┘                  └──────────┘
```

**状态说明表**：

| 状态名称 | 状态含义 | 进入条件 | 可触达的状态 |
|----------|----------|----------|-------------|
| 状态1 | [说明] | [初始条件] | 状态2, 状态4 |
| 状态2 | [说明] | [条件A] | 状态3 |
| ... | ... | ... | ... |

---

### 3. 详细功能说明 (What - 微观)

> 填写指引：这是 PRD 篇幅最大、最详细的部分，需要结合页面原型逐一讲解。

---

#### 3.1 页面：[页面名称]（页面ID: XX）

##### 3.1.1 前置条件

> 填写指引：用户进入该页面或触发该功能需要满足什么条件？

| 条件项 | 说明 |
|--------|------|
| 登录状态 | 必须登录 / 无需登录 / 部分可见 |
| 角色权限 | [如：仅VIP用户可见] |
| 其他条件 | [如：必须已绑定手机号、必须完成实名认证] |

##### 3.1.2 页面布局

> 填写指引：用 ASCII 框图描述页面布局，标注每个区域的功能。

```
┌─────────────────────────────────┐
│  [顶部区域说明]                  │
├─────────────────────────────────┤
│  [中间区域说明]                  │
│  ├─ [子区域A]                   │
│  └─ [子区域B]                   │
├─────────────────────────────────┤
│  [底部区域说明]                  │
└─────────────────────────────────┘
```

##### 3.1.3 页面展示字段汇总（选填）

> 填写指引：如果页面展示字段较多，可在此汇总列出每个字段的说明。

| 区域 | 字段 | 说明 |
|------|------|------|
| {{区域名}} | {{字段名}} | {{字段说明}} |
| {{区域名}} | {{字段名}} | {{字段说明}} |
| {{区域名}} | {{字段名}} | {{字段说明}} |

##### 3.1.4 组件状态表

> 填写指引：每个有多个状态的组件必填，描述不同状态下的视觉表现。

| 组件编号 | 状态 | 视觉表现 | 触发条件 |
|----------|------|----------|----------|
| C-XX-01 | 正常 | [正常展示样式] | [数据加载成功] |
| C-XX-01 | 加载中 | [骨架屏/loading动画] | [数据请求中] |
| C-XX-01 | 空 | [空状态插画+文案] | [数据为空] |
| C-XX-01 | 错误 | [错误提示+重试按钮] | [接口异常] |
| C-XX-01 | 禁用 | [灰色/不可点击] | [不满足操作条件] |

##### 3.1.5 交互逻辑详述

> 填写指引：对该页面的关键交互进行详细描述。

| 交互编号 | 交互描述 | 触发元素 | 前置条件 | 处理逻辑 | 执行结果/跳转 |
|----------|----------|----------|----------|----------|-------------|
| INT-001 | [如：点击提交按钮] | C-XX-03 | [如：表单已完整填写] | [1.前端校验 → 2.调API00X → 3.成功后跳转] | [页面YY / toast提示 / 弹窗] |
| INT-002 | [交互描述] | [组件编号] | [前置条件] | [处理步骤] | [结果] |

> 重复 3.1 节，为本功能覆盖的每个 C 端页面继续填写 3.2、3.3...

---

#### 3.N 后台管理：{{管理页面名称}}（选填）

> 填写指引：如果本功能涉及后台运营管理页面，在此完整描述。后台系统为独立运营后台（如 admin.xxx.com）。

##### 3.N.1 页面：{{列表页名称}}

**前置条件**：

| 条件项 | 说明 |
|--------|------|
| 登录状态 | 必须登录（运营人员账号） |
| 角色权限 | 仅运营管理员角色可访问 |
| 其他条件 | 无 |

**页面布局**：

```
┌──────────────────────────────────────────────┐
│  [左侧导航栏]                                 │
├──────────────────────────────────────────────┤
│  {{页面标题}}                                 │
│  ┌────────────────────────────────────────┐  │
│  │ [操作按钮]                  共 N 条记录 │  │
│  ├────────────────────────────────────────┤  │
│  │ 列1 │ 列2 │ 列3 │ ... │ 状态 │ 操作    │  │
│  ├────────────────────────────────────────┤  │
│  │ ...                                     │  │
│  └────────────────────────────────────────┘  │
└──────────────────────────────────────────────┘
```

**页面交互**：

| 交互编号 | 交互描述 | 触发元素 | 前置条件 | 处理逻辑 | 执行结果/跳转 |
|----------|----------|----------|----------|----------|-------------|
| INT-ADM-01 | [操作描述] | [按钮/元素] | [条件] | [处理步骤] | [结果] |
| INT-ADM-02 | [操作描述] | [按钮/元素] | [条件] | [处理步骤] | [结果] |

##### 3.N.2 页面：{{表单页名称}}

**前置条件**：

| 条件项 | 说明 |
|--------|------|
| 登录状态 | 必须登录（运营人员账号） |
| 角色权限 | 仅运营管理员角色可访问 |
| 其他条件 | 编辑模式时需传入记录 ID |

**页面布局**：

```
┌──────────────────────────────────────────────┐
│  [左侧导航栏]                                 │
├──────────────────────────────────────────────┤
│  ← 返回列表         {{表单标题}}              │
│  ┌────────────────────────────────────────┐  │
│  │                                        │  │
│  │  字段1 *                               │  │
│  │  ┌──────────────────────────────────┐  │  │
│  │  │ [输入框/上传/选择]                │  │  │
│  │  └──────────────────────────────────┘  │  │
│  │                                        │  │
│  │  字段2 *                               │  │
│  │  ┌──────────────────────────────────┐  │  │
│  │  │ [输入框/上传/选择]                │  │  │
│  │  └──────────────────────────────────┘  │  │
│  │                                        │  │
│  │          [  保存  ]   [  取消  ]       │  │
│  └────────────────────────────────────────┘  │
└──────────────────────────────────────────────┘
```

**页面交互**：

| 交互编号 | 交互描述 | 触发元素 | 前置条件 | 处理逻辑 | 执行结果/跳转 |
|----------|----------|----------|----------|----------|-------------|
| INT-ADM-F01 | [操作描述] | [按钮/元素] | [条件] | [处理步骤] | [结果] |
| INT-ADM-F02 | [操作描述] | [按钮/元素] | [条件] | [处理步骤] | [结果] |
| INT-ADM-F03 | 取消 | [取消] 按钮 | - | 返回列表页，不保存任何修改 | 列表页 |

**表单校验规则**：

| 字段 | 校验规则 | 错误提示 |
|------|----------|----------|
| {{字段名}} | 必填，最多 {{N}} 字 | "{{错误提示文案}}" |
| {{字段名}} | 必填，格式限 {{格式要求}}，单张 ≤ {{N}}MB | "{{错误提示文案}}" |
| {{字段名(条件显示)}} | 必填（仅 {{条件}} 时显示和校验） | "{{错误提示文案}}" |

---

#### 3.N+1 数据流（本功能相关）

> 填写指引：描述本功能涉及的数据如何获取、流转、更新。

| 数据项 | 来源 | 更新时机 | 缓存策略 | 前端存储 |
|--------|------|----------|----------|----------|
| [数据名] | API00X | 页面加载/定时轮询/用户操作 | [缓存N分钟] | [Store/LocalStorage/无] |
| [数据名] | 本地 | 用户操作 | - | [本地状态] |
| ... | ... | ... | ... | ... |

---

#### 3.N+2 接口规格（本功能相关）

##### 接口列表

> 填写指引：列出本功能涉及的所有接口。

| 接口ID | 接口名称 | 请求方式 | 接口路径 | 调用页面 | 调用时机 | 是否需要登录 |
|--------|----------|----------|----------|----------|----------|-------------|
| API001 | [接口名] | GET/POST | /api/xxx | XX | 页面加载 | 是/否 |
| API002 | [接口名] | GET/POST | /api/xxx | XX | 用户点击 | 是/否 |
| ... | ... | ... | ... | ... | ... | ... |

##### 接口详情

> 填写指引：每个接口一个子节，包含完整请求/响应/错误码。

---

**API001：[接口名称]**

**调用场景**：

| 项目 | 说明 |
|------|------|
| 触发页面 | [页面ID] |
| 触发时机 | [页面加载 / 用户点击 / 定时轮询 / 下拉刷新] |
| 调用频率 | [如：每次页面加载调用1次] |

**鉴权方式**：

| 是否需要登录 | 鉴权方式 | Token位置 |
|-------------|----------|-----------|
| 是 / 否 | [无 / Bearer Token / Cookie] | [Header Authorization / Cookie / -] |

**请求参数**：

| 参数名 | 类型 | 必填 | 默认值 | 说明 |
|--------|------|------|--------|------|
| [参数名] | String/Number/Enum | 是/否 | [默认值] | [说明] |
| ... | ... | ... | ... | ... |

**响应数据结构**：
```json
{
  "code": 0,
  "message": "success",
  "data": {
    "[字段名]": "[类型，示例值]",
    "[字段名]": "[类型描述]"
  }
}
```

**前端渲染规则（选填）**：

| 规则 | 说明 |
|------|------|
| {{规则项}} | {{说明}} |
| {{规则项}} | {{说明}} |

**错误码**：

| 错误码 | 说明 | 前端处理 |
|--------|------|----------|
| [xxxx] | [错误描述] | [展示文案 / 跳转登录 / 重试] |
| ... | ... | ... |

---

> 重复以上接口详情结构，为本功能的每个接口继续填写。

---

#### 3.N+3 业务规则

> 填写指引：详细描述本功能涉及的业务规则，包括校验逻辑、计算逻辑、权限判断等。

| 规则编号 | 规则描述 | 触发条件 | 处理逻辑 | 异常处理 |
|----------|----------|----------|----------|----------|
| RULE-001 | [如：积分兑换必须积分余额 ≥ 所需积分] | [用户点击兑换时] | [前端先校验，后端再校验] | [前端toast提示"积分不足"，后端返回错误码xxxx] |
| RULE-002 | [规则描述] | [触发条件] | [处理逻辑] | [异常处理] |
| ... | ... | ... | ... | ... |

---

#### 3.N+4 定时任务 / 异步处理（本功能相关）

> 填写指引：如果本功能涉及定时任务或异步处理，在此描述。如不涉及，填"本功能不涉及定时任务或异步处理"。

| 任务编号 | 任务名称 | 执行频率 | 任务逻辑 | 失败处理 |
|----------|----------|----------|----------|----------|
| CRON-001 | [任务名] | [每N分钟/每天N点] | [做什么] | [失败重试/告警/记录日志] |
| ... | ... | ... | ... | ... |

---

> ═══════════════════════════════════════════════════════════
> 以上为功能 TX-X001 的完整规格。如本模块有多个功能，请复制上述结构继续填写 TX-X002、TX-X003...
> ═══════════════════════════════════════════════════════════



## 模块汇总

> 填写指引：用简短的总结性语句概括本模块的关键设计决策和注意事项，方便团队成员快速回顾。

> **核心说明**：
> 1. {{关键设计决策1}}
> 2. {{关键设计决策2}}
> 3. {{关键设计决策3}}
> 4. {{待办/注意事项}}

---

## 🔴 一致性检查清单

> 本模块规格填写完成后，逐项检查：

**A01 引用检查**：
- [ ] 本模块覆盖的功能ID 是否全部在 A01 功能清单中存在？
- [ ] 本模块覆盖的页面ID 是否全部在 A01 页面拓扑图中存在？
- [ ] 各功能交互表中的跳转目标是否都在 A01 页面拓扑图中存在？

**功能维度自检**（每个功能逐项检查）：
- [ ] 「1. 需求背景与目标」是否用一句话交代了"为什么要做"？
- [ ] 「2.1 用户路径图」是否清晰展示了完整的用户操作路径？
- [ ] 「2.2 状态流转图」是否覆盖了所有业务状态及其转换条件？
- [ ] 「3.1 页面规格」每个页面是否有完整的布局图 + 组件状态表 + 交互逻辑？
- [ ] 「3.N 后台管理」如有涉及，列表页 + 表单页 + 校验规则是否完整？
- [ ] 「数据流」是否覆盖本功能所有数据需求？
- [ ] 「接口规格」每个接口是否有完整的请求/响应/错误码定义？
- [ ] 「业务规则」是否涵盖了所有校验、计算、权限规则？
- [ ] 「定时任务」是否已评估（无则填写"本功能不涉及"）？

**A02 引用检查**：
- [ ] 本模块如有后台配置项，是否在 A02 管理后台中有对应管理页面？

---

> **上游依赖**：《A01_用户侧全局说明》中定义的功能ID和页面ID

---

## ⚠️ 待确认事项

> 填写指引：列出填写过程中发现的模糊点或需要产品经理/运营确认的问题，逐项标注当前假设，等待校对确认。

1. **{{待确认问题1}}**：{{问题描述和当前假设}}
2. **{{待确认问题2}}**：{{问题描述和当前假设}}
3. **{{待确认问题3}}**：{{问题描述和当前假设}}

> 请逐项校对，有需要修改或补充的直接告诉我！
